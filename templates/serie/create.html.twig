{% extends 'base.html.twig' %}

{% block title %}
    Ajouter une series | {{ parent() }}
{% endblock %}

{% block body %}
    {{ form_start(serieFrm) }}
    {{ form_errors(serieFrm) }}
    {{ form_widget(serieFrm) }}
    <button>Create</button>
    {{ form_end(serieFrm) }}
{% endblock %}

